spring:
  application:
    name: books
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100
  h2:
    console:
      enabled: true